rootProject.name = "WorldTools"
pluginManagement {
    repositories {
        maven("https://maven.fabricmc.net/")
        maven("https://maven.architectury.dev/")
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "WorldTools"
include("fabric")
